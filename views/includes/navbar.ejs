<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI-Themed Navbar</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <!-- Google Fonts for a slightly more techy feel -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* --- General Body Styling --- */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #00132f; /* A light grey background to make the navbar pop */
            min-height: 200vh; /* Added height to demonstrate the sticky-top functionality */
        }

        /* --- Sleek AI Navbar Styling --- */
        .navbar-ai-sleek {
            /* Replace flat black with a deep, sophisticated tech blue */
            background-color: #0c0d0c !important; 
            border-bottom: 2px solid #00BFFF; /* A sharp, electric blue accent line */
            box-shadow: 0 4px 30px rgba(0, 123, 255, 0.25); /* A subtle glow */
        }

        /* Make your links fit the new theme */
        .navbar-ai-sleek .navbar-brand {
            color: #f5f3f3 !important; /* Make the brand match the accent color */
            font-weight: 700;
            transition: color 0.3s ease;
        }

        /* Style for all other links and text in the navbar */
        .navbar-ai-sleek .navbar-nav a, 
        .navbar-ai-sleek .navbar-text, 
        .navbar-ai-sleek a.text-decoration-none {
            color: #EAEAEA !important; /* Standard links are off-white for readability */
            font-weight: 500;
            transition: color 0.3s ease;
        }

        /* Hover effect for links */
        .navbar-ai-sleek a.text-decoration-none:hover,
        .navbar-ai-sleek .navbar-brand:hover {
            color: #DC3545 !important; /* Links brighten on hover */
            text-shadow:blueviolet ;
        }

        /* Keep logout/danger text red for user feedback consistency */
        .navbar-ai-sleek .text-danger {
            color: #DC3545 !important; 
          
        }

        /* Style the search input for the theme */
        .navbar-ai-sleek .form-control {
            background-color: #161B22;
            border: 1px solid #30363D;
            color: #EAEAEA;
        }

        .navbar-ai-sleek .form-control::placeholder {
            color: #8B949E;
        }

        .navbar-ai-sleek .form-control:focus {
            background-color: #0D1117;
            border-color: #cb2d2d;
            box-shadow: 0 0 0 0.25rem rgba(0, 191, 255, 0.25);
            color: #EAEAEA;
        }

        /* Improve the navbar toggler icon for dark backgrounds */
        .navbar-toggler-icon {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba%280, 191, 255, 0.8%29' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
        }
    </style>
</head>
<body>

    <!-- Your Navbar with the new AI class and cleaned-up styles -->
    <nav class="navbar navbar-expand-lg navbar-dark py-3 sticky-top navbar-ai-sleek" style="z-index: 1030;">
        <div class="container-fluid position-relative">
    
            <a class="navbar-brand d-flex align-items-center gap-2" href="/">
                <i class="fas fa-compass"></i> Explore
            </a>
    
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>
    
            <div class="position-absolute start-50 translate-middle-x d-none d-lg-block" style="width: 350px;">
                <form class="d-flex" id="search-form" action="/listings" method="GET">
                    <input
                        id="searchInput"
                        class="form-control me-2"
                        type="search"
                        name="search"
                        placeholder="Search destinations..."
                        aria-label="Search"
                    />
                </form>
            </div>
    
            <div class="collapse navbar-collapse" id="navbarContent">
                <div class="d-flex flex-column flex-lg-row align-items-lg-center ms-lg-auto gap-3 mt-3 mt-lg-0">
    
                    <a href="/listings/new" class="text-decoration-none">WanderLust creations</a>
    
                    <% if (typeof currUser !== 'undefined' && currUser) { %>
                        <a href="/bookings/mine" class="text-decoration-none">My Bookings</a>
                        <span class="navbar-text">Hi, <%= currUser.username %></span>
                        <a href="/logout" class="text-danger text-decoration-none">Logout</a>
                    <% } else { %>
                        <a href="/signup" class="text-decoration-none">Sign up</a>
                        <a href="/login" class="text-decoration-none">Log in</a>
                    <% } %>
    
                </div>
            </div>
        </div>
    </nav>

    <!-- Placeholder content to demonstrate scrolling -->
    <!-- <div class="container mt-5">
        <h1 class="mb-4" style="color: white;">Page Content</h1>
        <p style="color: white;">Scroll down to see the sticky navbar in action.</p>
    </div> -->

    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Your existing script for the search form
        const searchForm = document.getElementById("search-form");
        const searchInput = document.getElementById("searchInput");
    
        if (searchForm) {
            searchForm.addEventListener("submit", function (e) {
                const query = searchInput.value.trim();
                if (!query) {
                    e.preventDefault(); // prevent empty searches
                }
            });
        }

        // Example: Define a mock currUser object for demonstration purposes
        // In your actual EJS template, this would be passed from the server.
        const currUser = { username: 'Alex' }; 
        // To test the 'else' condition, set: const currUser = null;
    </script>

</body>
</html>
